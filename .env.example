# Example .env file for Automation-Script
# Copy this to `.env` and fill in real values before running the scripts.

#######################
# Google Gemini AI
#######################
# Your Gemini API key (required for AI calls)
GEMINI_API_KEY=your_gemini_api_key_here
# Gemini model to use (examples: gemini-1.5-flash, gemini-2.0-flash-exp, gemini-2.5-flash)
GEMINI_MODEL=gemini-1.5-flash
# Optional: temperature and token limits used by LangChain / direct genai calls
TEMPERATURE=0.1
MAX_TOKENS=8192

#######################
# Figma
#######################
# URL of the Figma file (example: https://www.figma.com/file/FILE_KEY/your-file-name)
FIGMA_FILE_URL=https://www.figma.com/file/FILE_KEY/your-file-name
# Figma API token (used by many scripts)
FIGMA_API_TOKEN=your_figma_api_token_here
# Alternative/alias used in some scripts
FIGMA_ACCESS_TOKEN=your_figma_access_token_here

#######################
# Project / Paths
#######################
# Path to the project theme (used to read/write theme files)
PROJECT_THEME_PATH=./generated-theme
# Generic project path used by some scripts
PROJECT_PATH=./project-root
# Specific path used by CPT generation scripts
PROJECT_PATH_FOR_CPT_GENERATION=./project-root
# Web project path (for WordPress/front-end projects)
WEB_PROJECT_PATH=./web-project
# Output folder name for generated WP theme (optional)
WP_THEME_OUTPUT_FOLDER=generated-wp-theme

#######################
# Boilerplate / Repo
#######################
# URL to clone/pull boilerplate (used by WordPress generators)
BOILERPLATE_REPO_URL="https://github.com/kombee-technologies/WordPress-boilerplate.git"

#######################
# Concurrency / Rate / Timeouts
#######################
# Delay (seconds) between AI / processing calls (default in many scripts)
PROCESSING_DELAY=3
# Page-specific processing delay used by Figma scripts
PAGE_PROCESSING_DELAY=3
# General iteration / retry counts
ITERATION_COUNT=3
# Maximum number of worker threads to use
MAX_WORKERS=4
MAX_THREADS=4
MAX_WORKER_THREADS=10
# Timeout (seconds) for downloads
DOWNLOAD_TIMEOUT=60

#######################
# Feature flags / Booleans
#######################
# Whether to treat header/footer as common across pages (true/false)
IS_COMMAN_HEADER_FOOTER=false
# Some scripts use a correctly-spelled variant; keep both as possible aliases
IS_COMMON_HEADER_FOOTER=false
# Multi-page mode (true/false)
MULTI_PAGE_MODE=true
# Whether to use Figma page names for project pages
USE_FIGMA_PAGE_NAMES=true

#######################
# Misc / Script-specific
#######################
# Path or name used for generated WordPress theme folder (may be modified by scripts)
WP_THEME_OUTPUT_FOLDER=generated-wp-theme
# Optional: some scripts expect a different var name for gemini key (lowercase used in dicts)
# but the canonical env var is GEMINI_API_KEY above.

# End of .env.example